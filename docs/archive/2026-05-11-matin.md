# Session matin — 11 mai 2026

## Contexte
Pause de 9h apres session marathon du 10 mai (24 commits Home +
Nav + Training). Reprise sur les zones restantes de Training :
WorkoutSession (seance active + save workflow).

## Sprints livres (30 min, 2 commits + audit)

### N1.1 — WorkoutCelebration tokens (976c57a)
- 43 -> 40 lignes
- Suppression 5 const hardcoded (#3B82F6, #22C55E, #F97316,
  #F5F5F5, #6B7280)
- CONFETTI_COLORS depuis tokens (gold, success, orange, blue, error)
- Award icon : GREEN -> colors.gold (coherent brand premium)
- Font 'Barlow Condensed' -> fonts.alt
- Animation framer-motion intacte
- **Validation visuelle : en attente seance gym live cet aprem**

### N1.4 — Done screen tokens + Lucide icons (1f2c776)
- 25 lignes refondues dans WorkoutSession.tsx (L593-617)
- Titre 'TERMINE fire' -> 'TERMINE' (Trophy icon gold suffit)
- Stats icons : emojis -> Lucide (Clock, CheckCircle2, Dumbbell)
- Stats cards + recap cards : BG_CARD -> surface2 + divider
- **Validation visuelle : en attente seance gym live cet aprem**

## Audit complet livre
- WorkoutSession.tsx : 1250 lignes, 13 UI layers identifies
- 2 flows de fin distincts identifies :
  - Flow A : WorkoutSession fullscreen (DEMARRER -> TERMINER -> save)
  - Flow B : TrainingTab inline (sets coches -> TERMINER -> save)
- Composants concernes :
  - WorkoutSession (1250 lignes, gym-critique)
  - WorkoutCelebration (43 -> 40 lignes, refondu N1.1)
  - TrainingSessionDone (56 lignes, deja coherent v2)

---

## Roadmap WorkoutSession refonte (3 sprints restants)

### N1.2 — WorkoutSession backgrounds [PROCHAINE SESSION, M effort 1-2h]
Priorite : P1
Scope : remplacer 18x BG_CARD -> colors.surface2 + 14x BG_BASE ->
colors.background + borders hardcoded -> colors.divider.

Risque : MOYEN (gym-critique, 32 remplacements a valider visuellement
sur chaque etat UI).

Strategie : audit cible d'abord (lister les 32 occurrences avec
contexte), puis remplacements groupes par etat UI (overview,
exercice actif, rest timer, save modals).

Test obligatoire : lancer une seance complete apres refonte
(DEMARRER -> exercice -> rest -> next exercice -> TERMINER).

### N1.3 — WorkoutSession hardcoded colors [APRES N1.2, M effort 1-2h]
Priorite : P1
Scope : ~65 occurrences rgba hardcodees + 8x #C9A84C -> colors.gold.

Risque : MOYEN (fragile si token change, mais purement cosmetique).

Strategie : prioriser les couleurs les plus utilisees (gold,
success, error, warning) puis nettoyer les rgba opacity.

Test : visuel sur tous les etats UI apres refonte.

### N1.5 — Modals harmonisation [APRES N1.2, M effort 1h]
Priorite : P2
Scope : 5 modals dans WorkoutSession avec backdrops differents
(rgba 0.85 / 0.9 / 0.92 / 0.95) -> token unique ou uniformise.

Modals concernes :
- showEndModal (modal TERMINER)
- showSaveTemplate (sauver comme template)
- exerciseInfo (bottom sheet)
- variantPopup (bottom sheet)
- showVideo (video overlay)

Risque : MOYEN (5 modals a tester, chacun = 1 flow a reproduire).

## Estimation globale roadmap

Total restant : ~3h30 de travail concentre (idealement sur 2
sessions de demi-journee).

Ordre recommande : N1.2 (backgrounds, base solide) -> N1.3 (couleurs,
coherence finale) -> N1.5 (modals, polish dernier).

Apres ces 3 sprints, Workout Session sera 100% v2-aligned et la
refonte Training Tab completement terminee.
